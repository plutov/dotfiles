# Agent instructions

## Pi defaults

- Prefer using the `pi-subagents` skill for multi-step, parallelizable, or review-heavy tasks.
- For simple one-step tasks, avoid unnecessary delegation.

## My personal code preference

- Write concise code, avoid unnecessary checks
- Do not add ANY comments, I will add them manually if I need to

## Writing tests

- Do not create useless tests

## Git

- Do not run any Git commands except `git diff`.
- Do not commit or push code; I handle all Git actions myself.
