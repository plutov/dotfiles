## dotfiles

Apply:

```bash
./dotfiles.sh -a
```

Install tools:

```bash
./dotfiles.sh -i
```

Save:

```bash
./dotfiles.sh -s
```

## macos

Manual settings:
- Automatically hide and show the menu bar -> Always
