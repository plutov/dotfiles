## dotfiles

Alex's dotfiles and setup code for his macos.

Apply:

```bash
./dotfiles.sh -a
```

Install tools:

```bash
./dotfiles.sh -i
```

Save:

```bash
./dotfiles.sh -s
```

## macos

Manual settings:
- Automatically hide and show the menu bar -> Always
